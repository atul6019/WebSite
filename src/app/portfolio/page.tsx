import { SectionHeader } from "@/components/SectionHeader";
import { caseStudies } from "@/data/portfolio";
import { pageMetadata } from "@/lib/seo";
export const metadata = pageMetadata("Portfolio", "Case studies for industrial IoT, relay controllers, monitoring systems, LED displays, access control, and AI cameras.", "/portfolio");
export default function PortfolioPage(){return <main className="container-pad py-20"><SectionHeader kicker="Portfolio" title="Industrial product case studies"/><div className="grid gap-6 md:grid-cols-2">{caseStudies.map((c)=><article className="tech-card" key={c.slug}><h2 className="text-2xl font-bold text-white">{c.title}</h2><h3 className="mt-5 font-semibold text-accent">Problem</h3><p className="mt-2 text-slate-400">{c.problem}</p><h3 className="mt-5 font-semibold text-accent">Solution</h3><p className="mt-2 text-slate-400">{c.solution}</p><h3 className="mt-5 font-semibold text-accent">Technologies Used</h3><div className="mt-2 flex flex-wrap gap-2">{c.technologies.map((t)=><span className="rounded-full bg-cyan-300/10 px-3 py-1 text-sm text-cyan-100" key={t}>{t}</span>)}</div><h3 className="mt-5 font-semibold text-accent">Results</h3><ul className="mt-2 list-inside list-disc text-slate-400">{c.results.map((r)=><li key={r}>{r}</li>)}</ul></article>)}</div></main>}
